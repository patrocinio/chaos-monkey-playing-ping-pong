apiVersion: v1
kind: Service
metadata:
  name: {{ template "ping-pong.fullname" . }}
  labels:
    app: {{ template "ping-pong.name" . }}
    chart: {{ template "ping-pong.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "ping-pong.name" . }}
    release: {{ .Release.Name }}
